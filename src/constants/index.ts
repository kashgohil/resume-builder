export enum TemplateType {
  DEFAULT = "DEFAULT",
  SIMPLE = "SIMPLE",
}

export enum TemplateSectionType {
  HEADER = "HEADER",
  EXPERIENCE = "EXPERIENCE",
  SKILLS = "SKILLS",
  PROJECTS = "PROJECTS",
  CUSTOM = "CUSTOM",
  FOOTER = "FOOTER",
}
